// Legacy ThemeToggle replaced with semantic + three-theme aware toggle.
// Prefer importing ThemeToggle from root (src/ThemeToggle.jsx) which uses ThemeProvider.
export { ThemeToggle as default } from '../ThemeToggle'
